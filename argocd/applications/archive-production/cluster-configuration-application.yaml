apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: archive-production-configuration
  namespace: argocd
spec:
  project: default # Todo define a project to limit the deployment types
  source:
    repoURL: https://gitlab.softwareheritage.org/infra/ci-cd/k8s-clusters-conf.git
    targetRevision: master
    path: archive-production
    directory:
      recurse: true
  destination:
    server: https://rancher.euwest.azure.internal.softwareheritage.org/k8s/clusters/c-2mwgb
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
