---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: swh-azure-storage-registry-credentials-vss
  namespace: gitlab-system
spec:
  destination:
    create: true
    name: swh-azure-storage-registry-credentials
    overwrite: true
  mount: secrets-gitlab-staging
  path: swh-azure-storage-registry-credentials
  refreshAfter: 10s
  type: kv-v2
  vaultAuthRef: openbao-vso-auth
